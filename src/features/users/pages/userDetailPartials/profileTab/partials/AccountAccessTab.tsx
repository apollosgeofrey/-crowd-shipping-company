// pages/users/tabs/AccountAccessTab.tsx
// import React from "react";

export default function AccountAccessTab({
    // props
}: {
    // props types
}) {
    // logic
    // props destructuring (if any)

    // helper functions (if any)

    // state (if any)

    // side effects (if any)

    // constants (if any)

    // computed values (if any)

    // event handlers (if any)

    // other logic (if any)
    
    // Render
    return (
        <div className="card border-0 shadow-sm rounded">
            <div className="card-body text-center">
                <p className="text-muted">No account access information available.</p>
            </div>
        </div>
    );
}
